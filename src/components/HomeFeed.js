const React = require("react");

function HomeFeed()  {
    return (
        <main>
        <img src="../public/HomeFeed.gif" alt="GIF welcoming user to home feed"/>
        </main>
    );
}

export default HomeFeed;